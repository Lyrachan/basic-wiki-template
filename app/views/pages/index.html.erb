<p style="color: green"><%= notice %></p>

<h1>Pages</h1>

<div id="pages">
  <% @pages.each do |page| %>
    <!-- <%= render page %> -->
    <p>
      <%= link_to "#{page.title}", page %>
    </p>
    <ul>
      <%= link_to "Ver artículos", articles_path, class: "btn btn-primary text-white" %>
        <% @articles.each do |article| %>
          <li>
            <%= link_to "#{article.title}", article_path(page), class: "btn btn-primary text-white" %>
          </li>
        <% end %>
    </ul>
  <% end %>
</div>

<%= link_to "New page", new_page_path %>
